export interface Feedback {
    scriptId: String,
    receiverId: String,
    giverId: String,
    completed: Boolean,
    dueOn: Number,
    notes: String,
    scriptRating: Number,
    feedbackRating: Number
}
