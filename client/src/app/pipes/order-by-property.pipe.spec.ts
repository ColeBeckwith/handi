import { OrderByPropertyPipe } from './order-by-property.pipe';

describe('OrderByPropertyPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByPropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
